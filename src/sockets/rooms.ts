export const getProjectRoom = (projectId: string): string => `project:${projectId}`;

export const getUserRoom = (userId: string): string => `user:${userId}`;

export const PRESENCE_ROOM = 'presence';
